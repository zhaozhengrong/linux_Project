# 环境搭建
## 1.安装opencv库
```base
  sudo apt install libopencv-dev
```

## 2.下载NCNN源码
```base
  git clone https://github.com/Tencent/ncnn.git
```
下载完之后进入到当前目录执行`cmake .`生成makefile
```base
  cmake .
```
然后开始编译
```base
  make -j$(nproc)
```
